---
interface Props {
	id: string;
}

const { id } = Astro.props;
---

<span class="sidenote-wrapper">
	<label for={`sn-${id}`} class="sidenote-number"></label>
	<input type="checkbox" id={`sn-${id}`} class="sidenote-toggle" />
	<span class="sidenote">
		<slot />
	</span>
</span>
